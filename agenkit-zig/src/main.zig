/// Placeholder main.zig - not used by default
///
/// To run examples, use:
///   zig build example
const std = @import("std");

pub fn main() !void {
    std.debug.print("Agenkit library - use examples instead\n", .{});
    std.debug.print("Run: zig build example\n", .{});
}
