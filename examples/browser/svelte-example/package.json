{
  "name": "agenkit-svelte-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-wasm": "npm --prefix ../../../packages/wasm run build && node ../copy-wasm.mjs",
    "predev": "npm run prepare-wasm",
    "dev": "vite",
    "prebuild": "npm run prepare-wasm",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "svelte": "^5.56.8",
    "typescript": "^5.5.0",
    "vite": "^6.4.3"
  }
}
