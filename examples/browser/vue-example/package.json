{
  "name": "agenkit-vue-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-wasm": "npm --prefix ../../../packages/wasm run build && node ../copy-wasm.mjs",
    "predev": "npm run prepare-wasm",
    "dev": "vite",
    "prebuild": "npm run prepare-wasm",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.4.3",
    "vue-tsc": "^2.0.0"
  }
}
