# Feature Parity Matrix

**Generated**: {{ generated_at }}

This matrix shows feature implementation status across all 6 Agenkit language implementations.

**Legend**:
- ✅ **Implemented** - Feature present in language
- ❌ **Missing** - Feature not yet implemented
- 🔧 **Partial** - Implementation exists but incomplete (future)

---

## Summary Statistics

| Language | Total Features | Parity % | Test Coverage |
|----------|----------------|----------|---------------|
{%- for stat in summary_stats %}
| {{ stat.language.ljust(10) }} | {{ stat.total_features }} | {{ stat.parity_percent }}% | {{ stat.test_count }} tests |
{%- endfor %}

---

## Feature Matrix by Category

{% for category in categories %}
### {{ category.replace('_', ' ').title() }}

{% set category_features = matrix_rows | selectattr('category', 'equalto', category) | list %}
{% if category_features %}
| Feature | Python | Go | TypeScript | Rust | C++ | Zig |
|---------|--------|----|-----------| -----|-----|-----|
{%- for row in category_features %}
| {{ row.feature }} | {{ row.languages.python }} | {{ row.languages.go }} | {{ row.languages.typescript }} | {{ row.languages.rust }} | {{ row.languages.cpp }} | {{ row.languages.zig }} |
{%- endfor %}

**Totals**:
{%- for summary in category_summaries %}
{%- if summary.category == category %}
Python: {{ summary.counts.python }}, Go: {{ summary.counts.go }}, TypeScript: {{ summary.counts.typescript }}, Rust: {{ summary.counts.rust }}, C++: {{ summary.counts.cpp }}, Zig: {{ summary.counts.zig }}
{%- endif %}
{%- endfor %}
{% else %}
*No features detected in this category.*
{% endif %}

---

{% endfor %}

## Category Breakdown

| Category | Python | Go | TypeScript | Rust | C++ | Zig |
|----------|--------|----|-----------| -----|-----|-----|
{%- for summary in category_summaries %}
| {{ summary.category.replace('_', ' ').title() }} | {{ summary.counts.python }} | {{ summary.counts.go }} | {{ summary.counts.typescript }} | {{ summary.counts.rust }} | {{ summary.counts.cpp }} | {{ summary.counts.zig }} |
{%- endfor %}

---

## Insights

{% set python_total = summary_stats[0].total_features %}
{% set go_total = summary_stats[1].total_features %}
{% set ts_total = summary_stats[2].total_features %}
{% set rust_total = summary_stats[3].total_features %}
{% set cpp_total = summary_stats[4].total_features %}
{% set zig_total = summary_stats[5].total_features %}

### Top Performing Languages (by feature count)
{%- if go_total >= python_total * 0.9 %}
1. **Go** - {{ go_total }} features ({{ summary_stats[1].parity_percent }}% parity) - Excellent coverage!
{%- endif %}
{%- if ts_total >= python_total * 0.7 %}
2. **TypeScript** - {{ ts_total }} features ({{ summary_stats[2].parity_percent }}% parity) - Strong implementation
{%- endif %}
{%- if rust_total >= python_total * 0.5 %}
3. **Rust** - {{ rust_total }} features ({{ summary_stats[3].parity_percent }}% parity) - Growing rapidly
{%- endif %}

### Areas for Improvement
{%- if rust_total == 0 %}
- **Rust**: Scanner not yet implemented (Phase 3)
{%- endif %}
{%- if cpp_total == 0 %}
- **C++**: Scanner not yet implemented (Phase 3)
{%- endif %}
{%- if zig_total == 0 %}
- **Zig**: Scanner not yet implemented (Phase 3)
{%- endif %}

{% if has_test_data %}
### Test Coverage Highlights
{%- for stat in summary_stats %}
{%- if stat.test_count > 0 %}
- **{{ stat.language.title() }}**: {{ stat.test_count }} tests
{%- endif %}
{%- endfor %}
{% else %}
*Test coverage data not available. Run: `./scripts/test-parity.sh` to generate.*
{% endif %}

---

## Next Steps

1. **Phase 3**: Implement Rust, C++, and Zig scanners to complete parity tracking
2. **Gap Analysis**: Review `GAPS_ANALYSIS.md` for specific missing features
3. **CI Integration**: Add parity validation to prevent regressions

---

*Generated by: `scripts/parity/matrix_generator.py`*
*Feature data from: `feature-manifest.json`*
{% if has_test_data %}*Test data from: `test-parity-report.json`*{% endif %}
